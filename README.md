# CustomFont

Replace font with a `.ttf` or `.otf` font. Built for both players and developers.

## Player Usage

Place `font.ttf` or `font.otf` in the folder.

## Developer Usage

See [here](http://localhost).

## Build

### Doc

```sh
dotnet docfx docfx.json --server
```